html
  head
    title= title
  body
    h1= message
    - for (var i=0; i < people.length;i++)
      div= people[i].name 
        | (
        - for (var j = 0; j < people[i].classes.length; j++)
          div= people[i].classes[j]
        | )
        
